#!/bin/bash

#CONTAINER=$(docker container list -a | awk '{print $14}')

#Start the download of the ansible container
#sudo docker run -ti cyberxsecurity/ansible bash

#display known containers
#docker container list -a | awk '{print $14}'

#Start the container and attach the container
#sudo docker start ${CONTAINER} && sudo docker attach ${CONTAINER}
